# Supabase Configuration
# Get these values from your Supabase project settings at https://supabase.com/dashboard

# Public anonymous key (safe to expose in client-side code)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (server-side only, never expose to client)
# Only needed for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Mapbox Configuration
# Get your public access token from https://account.mapbox.com/access-tokens/
# This token is safe to expose in client-side code
PUBLIC_MAPBOX_ACCESS_TOKEN=your-mapbox-access-token-here
